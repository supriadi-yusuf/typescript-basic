{
  "name": "basic",
  "version": "1.0.0",
  "description": "npm init tsc --init --sourceMap --rootDir src --outDir dist",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}
